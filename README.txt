使用方式：
1) 解壓後，將四個檔案（index.html / app.js / sw.js / ronin.overrides.css）覆蓋到你的專案資料夾。
2) 完全關掉 PWA / 頁籤後再打開（或強制重新載入），等待新快取生效。

驗收：
- 列表頁與 UI 保持深色不變。
- 文章閱讀頁右上有 🌙；短按循環：紙質 / 護眼(亮) / 護眼(夜)。
- 背景只影響文章內容區，CSV/書籤/搜尋等功能不變。
