【一步覆蓋安裝】
1) 解壓本包四個檔案（index.html / app.js / sw.js / ronin.overrides.css），覆蓋到你的網站資料夾。
2) 完全關掉 PWA / 分頁，重新開啟（或強制重新載入）。

【你會看到】
- 右上出現 🌙 按鈕（短按循環切換：紙質 / 護眼亮 / 護眼夜；長按展開三點選單）。
- 只有「文章閱讀區」背景與文字會切換；列表 / UI 維持深色。

【說明】
- app.js 已內建主題切換與保險用 inline CSS。
- ronin.overrides.css 僅作用在閱讀容器；不會污染列表。
- sw.js 使用新的快取名，保證抓到最新檔案。
